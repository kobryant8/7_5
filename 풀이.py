# 📘 추가 개념 복습 퀴즈 10문제 🧪  
# ───────────────────────────────────────────────

# ❓ Q1. CSV 파일을 읽을 때 첫 번째 열을 인덱스로 지정하려면 어떻게 해야 할까요?
# ① index_col=0  o
# ② set_index(0)  x
# ③ header=None  x
# ④ usecols=0  x

# ───────────────────────────────────────────────

# ❓ Q2. `.set_index()` 메서드는 언제 사용하는 것이 적절한가요?
# ① 파일을 읽기 전에  x
# ② 파일을 압축할 때  x
# ③ 데이터를 불러온 후 특정 열을 인덱스로 설정할 때  o
# ④ HTML에서 표를 찾을 때  x

# ───────────────────────────────────────────────

# ❓ Q3. `.transpose()`의 주요 기능은 무엇인가요?
# ① 데이터를 삭제한다  x
# ② 데이터를 정렬한다  x
# ③ 행과 열을 바꾼다  o
# ④ 데이터를 병합한다  x

# ───────────────────────────────────────────────

# ❓ Q4. 다음 중 `session_state`의 역할은 무엇인가요?
# ① 그래프를 출력한다  x
# ② 버튼을 클릭하게 한다  x
# ③ 사용자 상태 값을 저장하고 유지한다  o
# ④ 파일을 다운로드한다  x

# ───────────────────────────────────────────────

# ❓ Q5. 아래 중 Streamlit에서 상태값을 최초로 초기화하는 올바른 방식은?
# ① st.session_state = {}  x
# ② st.slider("라벨")  x
# ③ if '값' not in st.session_state: ...  o
# ④ session_state.save()x

# ───────────────────────────────────────────────

# ❓ Q6. Pie Chart에서 `.squeeze()`를 사용하는 이유는?
# ① 데이터를 압축해서 저장하기 위해  x
# ② DataFrame → Series로 변환하여 1차원으로 만들기 위해  o
# ③ 범례 생성을 위해  x
# ④ 라벨을 붙이기 위해  x

# ───────────────────────────────────────────────

# ❓ Q7. Streamlit에서 `st.download_button()` 사용 시 반드시 지정해야 하는 항목은?
# ① label  o
# ② file_name  o
# ③ mime o
# ④ 모두 필요함o

# ───────────────────────────────────────────────

# ❓ Q8. MIME 타입 `"text/csv"`의 목적은?
# ① 텍스트 파일로 저장되도록 하기 위함  x
# ② 파일이 CSV 형식임을 브라우저에 알려주기 위해  o
# ③ 파일 크기를 줄이기 위해  x
# ④ JSON 형식으로 변환하기 위해  x

# ───────────────────────────────────────────────

# ❓ Q9. 다음 중 슬라이더 범위를 기억하고 연동하기 위한 적절한 코드는?
# ① st.slider("년도")  x
# ② st.session_state.slider_value  o
# ③ if st.button("적용")  x
# ④ st.download_button() x

# ───────────────────────────────────────────────

# ❓ Q10. `data1.filter(items=arr).transpose()`에서 `.filter()`의 역할은?
# ① 데이터 병합  x
# ② 특정 열만 선택  o
# ③ 데이터 무작위 섞기  x
# ④ 컬럼 이름 바꾸기  x

# ───────────────────────────────────────────────


# 📘 추가 개념 퀴즈 정답 검토 ✍️  
# ────────────────────────────

# ✅ Q1. 정답: ① → 맞음  
# ✅ Q2. 정답: ③ → 맞음  
# ✅ Q3. 정답: ③ → 맞음  
# ✅ Q4. 정답: ③ → 맞음  
# ✅ Q5. 정답: ③ → 맞음  
# ✅ Q6. 정답: ② → 맞음  
# ✅ Q7. 정답: ④ → 맞음  
# ✅ Q8. 정답: ② → 맞음  
# ✅ Q9. 정답: ② → 맞음  
# ✅ Q10. 정답: ② → 맞음  

# 🎯 총점: **10 / 10**

# 완벽합니다! 모든 개념을 정확히 이해하셨고, 코드 기반 활용에서도 실수가 없습니다.
